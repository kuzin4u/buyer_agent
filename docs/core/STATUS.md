# buyer_agent — статус

> Репозиторий: `buyer_agent` · Версия: 0.2 · Дата: 2026-09-23
> Статус: сверено с кодом 2026-09-23 (`/verify`). Что сверить не удалось — помечено ⟦уточнить⟧.
> Обновлять в конце каждой рабочей сессии.


## Готово
- Ядро А: восемь функций 8.1–8.8, конвейер чеков ФНС, приём боевых выгрузок (`agent/intake.py`).
- Сценарий «продукты на неделю»: три варианта, выбор, список по магазинам, отправка в Telegram (`agent/plan.py`, `/plan`).
- Веб-оболочка (FastAPI), панель диагностики с пополнением словарей и замером эффекта (Р-23, Р-24).
- Умный слой с механической проверкой чисел, экспорт профиля, Telegram-бот, `docs/DEPLOY.md` (Р-26).
- Этапы по DECISIONS: С1 — схема экспорта, служебный префикс (Р-7); С3 — хвосты весовых; С5 — веб (Р-23);
  С6 — диагностика и пополнение словарей (Р-24); С7 — умный слой, экспорт, бот (Р-26).
  Состав С2 и С4 в документах не назван ⟦уточнить⟧.
- Тесты: 471 по прогону `tests/run.py --full` 2026-09-23 (быстрый набор ~30 с, полный — перед коммитом), `docs/BASELINE.txt` — эталон.
- Claude Code: CLAUDE.md, хук защиты SPEC.md с журналом `.claude/hooks.log`, команды
  start/finish/task/audit/verify/decision, подагент reviewer. Установка не закоммичена.

## В работе / следующее
- [ ] Хук после правки кода: запуск `python3 tests/run.py` (команда известна; в `settings.json` пока только PreToolUse).
- [ ] MCP-сервер над чеками: инструменты «найти покупки», «история цены»; каталог магазинов — ресурсом. В коде нет.
- [ ] Источник цен по рынку: открытые данные сетей / каталог Системы (SPEC §8.10, режим `Mode.ONE_OFF` заложен).
- [ ] П-9: 8.2 и 8.6 считают в разных деньгах — общего итога у варианта плана нет.
- [ ] Закоммитить установку Claude Code (`.claude/`, `docs/core/`, правки CLAUDE.md и .gitignore).

## Открытые вопросы
- Провайдер VPS: требования есть (Р-5, `docs/DEPLOY.md`), выбор был отложен до С7 и не сделан.
- Сопоставление товаров между сетями сейчас — правила и словари (SKU по Р-3, бренды, отсевы Р-21).
  Нужен ли метод сильнее (эмбеддинги / гибрид) для каталога рынка — не решено.

## Расхождения (документ ≠ код)
Проход `/verify` 2026-09-23. «Должно» — SPEC и DECISIONS, «есть» — код.

| # | Приоритет | Должно быть | Есть | Что сделать |
|---|---|---|---|---|
| 1 | важно | SPEC §3.3 и инвариант CLAUDE.md: «данные не покидают устройство пользователя» | При заданном `ANTHROPIC_API_KEY` умный слой отправляет во внешний API текст запроса и `facts()` — числа, названия групп и магазинов из ответа ядра (`agent/core/smart.py:213`, `:234`). Бот передаёт списки покупок через Telegram. В DECISIONS это исключение не записано | **Закрыто решением Р-29** (2026-09-23). |
| 2 | позже | SPEC §3.3: хранение настроек — «localStorage (веб) или файл/БД (бот)» | Настройки на сервере ядра в SQLite `state.db`; бот не хранит ничего. Так решено в ОА-1, Р-4, Р-23 — код следует DECISIONS | Владельцу обновить формулировку SPEC §3.3 (SPEC защищён хуком) |
| 3 | позже | README: состояние пакета | README пишет «Бот — С6» (стр. 85) и «Телеграм-бота — С7» в разделе «Чего в пакете нет» (стр. 195), но `bot/` есть и работает (Р-26) | **Закрыто** 2026-09-23: README правлен. |
| 4 | позже | CLAUDE.md, «Структура репозитория» | «Flask-подобное приложение» — фактически FastAPI; «25 тестов» — 24 модуля `test_*.py` + `fixture.py` | **Закрыто** 2026-09-23: CLAUDE.md правлен. |
| 5 | позже | Р-6: прогон `python3 -m unittest discover -s tests` | Основной прогон — `tests/run.py` (Р-26); комментарий в `requirements.txt` ссылается на старую команду | **Закрыто** 2026-09-23: комментарий ссылается на `tests/run.py`. |
| 6 | позже | Р-4: нормализованная таблица «кэшируется с ключом = хеш конфигов» | Кэш в памяти сессии по `Config.fingerprint`; каталог `.cache/` в `.gitignore` кодом не используется | **Закрыто** 2026-09-23: строка `.cache/` удалена из .gitignore. |
| 7 | позже | `agent/store.py`: комментарий к таблице `exports` | Комментарий про реестр выгрузок стоит над `outbox`, а не над `exports` | **Закрыто** 2026-09-23: комментарий перенесён к `exports`. |
| 8 | позже | Хук `protect_spec.py`: блокировать только запись в корневой SPEC-файл | Bash-команда блокируется, если в ней где угодно есть имя SPEC-файла и любая операция записи (`>`, `open(..., 'w')` и т. п.), даже когда пишется другой файл. Поймано на `/verify` 2026-09-23: скрипт правки `docs/core/INDEX.md`, где имя SPEC-файла было в тексте ссылки | Сузить проверку до цели записи; добавить случай в `test_protect_spec.py` |
| 9 | важно | Р-23: служебные слова не показываются — группы названиями из `categories.json`, периоды и разрезы по-русски | `facts()` отдаёт `id` групп и периодов: `agent/core/smart.py:286` (basket), `:301` (budget), `:310` (lapsed), `:363` (разрез spending), `:384` («корзина: на week»). Те же строки печатает бот в ответах `/api/ask`. Напоминания: `web/app.py:737` кладёт `item.group` без `gtitle` — в Telegram приходит «kefir — 53 дн назад». В списке покупок (`plan_message`) названия правильные. Замер `/verify` 2026-09-23 на копии `state.db` | Пропускать группы через `gtitle`, периоды и разрезы — через словарь оболочки; закрепить тестом, что в `facts()` и в `/api/notifications` нет `id` из `categories.json` |
