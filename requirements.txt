# Зависимости ВЕБ-ОБОЛОЧКИ. Ядро (agent/) от них не зависит и работает на одной
# стандартной библиотеке — это проверяется тем, что `python3 tests/run.py`
# проходит без установки чего-либо, тесты оболочки при этом пропускаются
# (DECISIONS Р-6, Р-26).
#
#     python3 -m venv .venv
#     .venv/bin/pip install -r requirements.txt
#     .venv/bin/python -m web            # http://127.0.0.1:8000
#
# Chart.js не здесь: он лежит в web/static/vendor/ файлом, чтобы оболочке не
# требовалась сеть для рисования графика.
fastapi==0.141.1
uvicorn[standard]==0.52.4
jinja2==3.1.6
python-multipart==0.0.32

# Умный слой §8.9. Необязателен: без ключа агент работает базовым слоем, и без
# самой библиотеки тоже — она импортируется лениво, только когда ключ задан.
anthropic==1.5.0

# Телеграм-бот: короткие сценарии и push (ОА-1, Р-1, Р-5).
aiogram==3.31.0

# Только для тестов оболочки (fastapi.testclient).
httpx==0.28.1
