"""Телеграм-бот: транспорт, а не агент (DECISIONS ОА-1, Р-1).

Три проверяемых запрета, и они здесь исполнены буквально:

1. **Бот не пишет на диск.** Ни базы, ни файлов, ни кэша.
2. **Бот не держит пользовательских данных между сообщениями.** Всё, что он
   знает, он спрашивает у ядра на каждый апдейт.
3. **Бот не содержит бизнес-логики.** Он форматирует ответ ядра и рисует кнопки.

Проверить это можно `tests/test_bot.py`: там прямо ищется обращение к диску, к
конфигам и к модулям `agent.*`. Единственная зависимость бота — HTTP API ядра.

Почему так строго. §3.3 требует, чтобы данные пользователя не расползались.
Стоит боту «кэшнуть профиль» или «запомнить шаг диалога» — и через два спринта
появляется вторая копия правды, которую никто не пересчитывает.
"""
